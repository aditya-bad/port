# Minimal — release builds currently ship with isMinifyEnabled=false (see
# app/build.gradle.kts), so nothing here is exercised yet. Kept as a
# placeholder for when that changes, at which point kotlinx.serialization's
# own rules (reflection-based decoding of the network models) will need to
# be added here.
